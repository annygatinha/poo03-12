2 Qual a diferença entre declarar um vetor fixo e inicializá-lo com valores específicos?
R = Quando você declara um vetor fixo, você apenas reserva um espaço na memória para uma quantidade específica de elementos, mas não define seus valores;
 já ao inicializá-lo você define o tamanho (ou deixa o compilador deduzir) e já atribui valores iniciais.
